interface:
  display_name: "WT Coordinate"
  short_description: "Coordinate running wt work"
  default_prompt: "Use $wt-coordinate to inspect running wt work, read agent status/surfaces, send feedback with `wt send`, review diffs/checks, complete workflow tasks when needed, and hand accepted work to $wt-land."
